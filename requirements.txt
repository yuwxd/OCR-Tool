# OCR Tool — Python dependencies
# Install: pip install -r requirements.txt

# === Core OCR ===
pytesseract>=0.3.10       # Python wrapper for Tesseract OCR engine
Pillow>=10.0.0            # Image loading and format support

# === Image preprocessing ===
opencv-python>=4.8.0      # Computer vision: denoising, binarization, deskew
numpy>=1.24.0             # Array operations for image data

# === CLI and terminal UI ===
typer>=0.9.0              # Modern CLI framework
rich>=13.5.0              # Colored output, progress bars, panels

# === Optional — clipboard support ===
pyperclip>=1.8.2          # --clipboard flag support

# ============================================================
# OPTIONAL — EasyOCR (deep-learning engine, better on complex text)
# Uncomment below to enable. Requires ~1.5 GB of disk for models.
# Run with: ocr image.png --easyocr
# ============================================================
# easyocr>=1.7.0
# torch>=2.0.0             # Required by EasyOCR
